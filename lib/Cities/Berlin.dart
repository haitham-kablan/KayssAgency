
import 'package:flutter/material.dart';

class BerlinPage extends StatelessWidget {
  const BerlinPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      
    );
  }
}
